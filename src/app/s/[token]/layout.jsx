export const runtime = 'edge'

export const metadata = {
  title: 'Shared Canvas',
  description: 'View a shared LixSketch canvas — E2E encrypted, decrypted in your browser.',
  robots: { index: false, follow: false },
}

export default function SharedSceneLayout({ children }) {
  return children
}
