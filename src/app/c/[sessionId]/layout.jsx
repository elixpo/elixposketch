export const runtime = 'edge'

export const metadata = {
  title: 'Canvas',
  description: 'LixSketch infinite canvas — draw diagrams with hand-drawn aesthetics, RoughJS rendering, and real-time collaboration.',
  robots: { index: false, follow: false },
}

export default function CanvasLayout({ children }) {
  return children
}
